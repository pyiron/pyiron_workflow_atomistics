"""Private plumbing — kwargs / dataclass / working-directory helpers.

Symbols here are NOT part of the public API and may move or be renamed
without deprecation. Use ``Engine.with_working_directory`` and the
``engine.*`` / ``structure.*`` / ``physics.*`` / ``analysis.*`` subpackages
instead.
"""
